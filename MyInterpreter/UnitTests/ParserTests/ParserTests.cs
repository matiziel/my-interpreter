using MyInterpreter.DataSource;
using MyInterpreter.Lexer.Tokens;
using Xunit;

namespace UnitTests.ParserTests
{
    public class ParserTests
    {
        private static TextPosition testPos = new TextPosition(0,0,0);
    }
}